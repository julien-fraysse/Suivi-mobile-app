/**
 * useSettings
 * 
 * Hook to access application settings.
 * Re-exports useSettings from SettingsContext for convenience.
 */
export { useSettings } from '../context/SettingsContext';
export type { Settings } from '../context/SettingsContext';

